// import { createApp, configureCompat } from "vue";
// import foo from "./foo.vue";

// // Enable specific Vue 2 features
// configureCompat({
// 	// Enable Vue 2 hooks like beforeCreate and created
// 	GLOBAL_MOUNT: true,
// 	INSTANCE_EVENT_EMITTER: true,
// 	INSTANCE_SET: true,
// 	INSTANCE_DELETE: true,
// });

// const container = document.querySelector(".container");

// if (container) {
// 	createApp(foo).mount(container);
// } else {
// 	console.error("Container not found");
// }

import SiteSyncLicense from "../../../multitracks/melodies/markup/markup.multitracks.com/js/main/page/site/site-sync-license.ts";

new SiteSyncLicense();
